# STYLE GUIDE — "Plein Air" 数字印象派作品集

身份概念：**一座会呼吸的自然画室**。所有项目共享同一套签名系统，拒绝一切"AI slop"默认美学（禁：Inter、紫蓝渐变、三栏图标卡片、均分配色、只有 fade 的动效）。

## 1. 签名元素（每个项目必须出现）

- **签章**：右下角一个手绘感笔触 SVG 签名（一小段朱红 #c8442a 不规则笔触 + JetBrains Mono 小字 "Plein Air — No.XX"），可共用同一段 SVG path，笔触带轻微透明度不均
- **纸感**：浅色页面叠纸张颗粒（程序生成噪点 canvas 或 SVG feTurbulence 滤镜，opacity 0.04–0.06）；深色页面叠胶片颗粒
- **编号**：Plein Air — No.01 … No.19（主页 No.00 可不用）

## 2. 调色板（莫奈画作取色，HSB 加权使用，禁止均匀随机取色）

- **干草堆 Haystack**：金 #d9a441 / 薰衣草影 #8d7fb8 / 米白 #f2e8cf / 深壤 #4a3b2a
- **睡莲 Waterlily**：塘绿 #2e4a3f / 叶 #a8c5a0 / 花 #e8b7c3 / 反光 #7ba8a0
- **暮色 Twilight**：夜蓝 #1c2340 / 雾蓝 #4a5a8a / 余烬 #e07b39 / 月白 #e8e4d8
- **晨雾 Fog**：雾白 #dfe3da / 灰绿 #9fb2a8 / 墨 #2a2e28
- 用法：主色 70% / 辅色 20% / 点缀 10%；色相做 ±5 高斯扰动；浅色页纸底用 #f2ede0（不用纯白）

## 3. 渲染技法库（每个项目至少用 2 种）

- **笔触粒子**：短锥形线段（头粗尾细），沿 curl noise/流场运动，方向随图像梯度，大笔触打底、逐层细化
- **水彩边缘**：色块边缘加深（edge darkening）+ 多层低透明度叠印，边缘用高斯扰动的多边形外扩
- **纸纹造粒**：feTurbulence 或噪点 canvas 叠加，mix-blend-mode: multiply / overlay
- **大气透视**：远景低饱和低对比 + 指数雾 + 轻暗角
- **光斑 komorebi**：叶隙光斑（柔和圆形光斑缓慢漂移）用于浅色页点睛

## 4. 动效原则（自然界的节奏，不是机器的节奏）

- 风是**一阵一阵的**（阵风扫过有方向有间歇），不是恒定流速
- 一切惯性与弹簧：k 小、阻尼大、带回弹；禁止线性 tween
- **状态系统**：每个项目有一个持续运转的世界状态（昼夜循环 / 天气 / 计数器 / 季节），"用户不看时世界也在动"
- 交互永不挡路：不做 scroll-jacking；动画可打断可跳过

## 5. 交互深度要求（每个项目 ≥3 层交互 + 1 个状态系统）

- 例：指针扰动（画/吹/拨）+ 键盘切换模式 + 点击/长按二级行为 + 时间系统
- 移动端：触摸等价物必须有；性能降档（粒子数减半、关后处理）
- 可选加分：WebAudio 程序化环境音（风/鸟/水，振荡器+噪声合成，默认关闭，角落留 toggle）

## 6. 性能预算

60fps 桌面 / 30fps 移动；DPR ≤ 2；instancing；页面隐藏暂停 rAF；后处理 pass 宁少勿多

## 7. 字体

Instrument Serif（标题/画作题字）/ Space Grotesk（正文）/ JetBrains Mono（数据/签章/编号）— 不变

## 8. 统一 topbar（保留原有 mix-blend-mode:difference 导航，编号改为 Plein Air 编号）
